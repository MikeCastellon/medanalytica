overwrite: true
schema:
  - https://api.heartquest-global.com/graphql:
      headers:
        x-hasura-admin-secret: yaRzaqnikkJa9zRBzmHLcXbfPx
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-query"
    config:
      fetcher: fetch
      endpoint: "https://api.heartquest-global.com/graphql"
  ./graphql.schema.json:
    plugins:
      - "introspection"
